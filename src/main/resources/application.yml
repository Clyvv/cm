spring:
   datasource:
      url: jdbc:mysql://localhost:3306/cm_db?createDatabaseIfNotExist=true&useSSL=true
      username: root
      password: password
      driverClassName: com.mysql.jdbc.Driver
   jpa:
      show-sql: true
      hibernate:
         ddl-auto: create
         naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
      properties:
         hibernate:
            dialect: org.hibernate.dialect.MySQL5InnoDBDialect
